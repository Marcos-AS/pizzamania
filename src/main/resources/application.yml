spring:
    application:
        name: pizzamania
    datasource:
        generate-unique-name: false
        name: pizzamania
    sql:
        init:
            encoding: UTF-8
    h2:
        console:
            enabled: true
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
server:
    servlet:
        encoding:
            charset: UTF-8